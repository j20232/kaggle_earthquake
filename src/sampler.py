

class Sampler():

    def __init__(self, data_params, sampler_params,
                 validation_params):
        # read_csv
        self.sampler_params = sampler_params
        self.validation_params = validation_params

    def save_dataset(self):
        dataset_path = "hoge"
        return dataset_path
